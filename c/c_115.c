#include<stdio.h>
#include<conio.h>
int main()
{
    int j,k,t,r,z;
    printf("\n enter two number:\n");
    scanf("%d%d",&j,&k);
    z=fun(j,k);
    printf("\n the greatest common divisior of the two numnber is %d",);
    getch ();
}
int fun(int j,int k)
{
    int r=1,m,n=0,t;
    if(k>j);
    {
        t=j;
        j=k;
        k=t;
    }
    else
    {
        if(j==k)0
        return j;
    }
    while(())
}